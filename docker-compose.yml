services:
  tob-calculator:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./outputs:/app/outputs
    environment:
      FLASK_HOST: "0.0.0.0"
      FLASK_DEBUG: "0"
      FLASK_PORT: "5000"
    restart: unless-stopped
